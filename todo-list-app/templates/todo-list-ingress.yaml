apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-list-ingress
spec:
  rules:
  - host: zima.ycinteclab.duckdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: todo-list
            port:
              number: 80
